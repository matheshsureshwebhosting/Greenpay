<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
</head>

<body>
    <div >
        <pre>
            Enter your Email    : <input type="text" name="email" id="email"> <br> 
            Enter your Name     : <input type="text" name="name" id="name"> <br> 
            Enter your Number   : <input type="number" name="mobile" id="mobile"> <br> 
            Enter your Password : <input type="password" name="password" id="password"> <br> 
            <input type="submit" value="Submit Form" id="signup-btn">  
        </pre>
    </div>
    <script src="https://cdnjs.cloudflare.com/ajax/libs/jquery/2.1.3/jquery.min.js"></script>
    <script>
        $('#signup-btn').click(function () {
            $.ajax({
                url: '/users',
                type: 'POST',
                cache: false,
                data: {
                    email: $('#email').val(),
                    name: $('#name').val(),
                    mobile: $('#mobile').val(),
                    password: $('#password').val(),                  
                },
                success: function (val) {
                    console.log(val)
                    // $('#error-group').css('display', 'none');
                    alert('Your submission was successful');
                },
                error: function (data) {
                    // $('#error-group').css('display', 'block');
                    var errors = JSON.parse(data.responseText);
                    // var errorsContainer = $('#errors');
                    // errorsContainer.innerHTML = '';
                    // var errorsList = '';

                    // for (var i = 0; i < errors.length; i++) {
                    //     errorsList += '<li>' + errors[i].msg + '</li>';
                    // }
                    console.log(errors)
                    // errorsContainer.html(errorsList);
                }
            });
        });
    </script>
</body>

</html>